const data = require("./data.json");

console.log(data);

console.log(data.address.street);

const watch = "node --watch filename";

console.log("To start watch mode, type next line in console: ", watch);